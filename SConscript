#vLibs = ['-lboost_random', '-lboost_regex', '-lboost_signals', 
#'-lboost_system', '-lboost_thread', '-lboost_filesystem', 
#'-lboost_program_options', '-lboost_date_time'
#];

#env = Environment()

#	Add header search path
#env.Append(CPPPATH = ['/usr/include'])

#	Add compile-time flags
#env.Append(CCFLAGS=[
#'-Wall','-g', '-lwthttp', '-lwt'])

#	Add library search path
#env.Append(LIBPATH = ['/usr/lib'])

#env.Program( Glob('exa.cc'), LIBS=vLibs)
#env.Program('hello',['exa.cc'], LIBS=vLibs)
